# おすすめのデプロイ手順

開発に慣れていない場合、Gitの履歴（コミット）とデプロイ内容がずれてしまうと混乱の原因になります。
以下の「3ステップ」をセットで行うのが、一番シンプルで安全な手順（お作法）です。

## 手順

### 1. 変更を保存（コミット）する
まず、手元の変更をGitに記録します。
```bash
git add .
git commit -m "変更内容のメモ"
```

### 2. GitHub本体とい同期（プッシュ）する
手元の記録をGitHubのリポジトリ（mainブランチ）にアップロードします。
```bash
git push origin main
```
※ これをやらないと、「デプロイされているサイトは最新だけど、GitHubのコード（main）は古い」というねじれた状態になってしまいます。

### 3. デプロイする
最後に、GitHub Pagesへ公開します。
```bash
npm run deploy
```

---

## まとめ
**「コミット」→「プッシュ」→「デプロイ」**
この順番を常に守れば、違和感なく開発を進められます！
